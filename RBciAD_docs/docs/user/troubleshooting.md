Troubleshooting

Blank plot → Check sfreq, channel selection, and node connections.

Slow UI → Use segment output or enable decimation.

Filter error → Increase filter length or relax transition bandwidth.

Connections not following → Update to the latest build (GUI fixes included).

No FIRST_FRAME → Inspect console for exceptions; disable heavy nodes.

LSL not found → Verify runtime install and stream name.